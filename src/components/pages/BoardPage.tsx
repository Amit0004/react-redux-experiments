import React from "react";
import ActionBoard from "../containers/ActionBoard";

export interface BoardPageProps {}

const BoardPage = ({}: BoardPageProps) => {
  return <ActionBoard></ActionBoard>;
};

export default BoardPage;
